import React from 'react';
import TestingPage from './testing';

// Unified into Testing Masterclass (SRP & Consolidation)
const TDDPage: React.FC = () => {
  return <TestingPage />;
};

export default TDDPage;
