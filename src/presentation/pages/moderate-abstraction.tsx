import React from 'react';
import AbstractionPage from './abstraction';

// Unified into Abstraction & API Design Masterclass (SRP & Consolidation)
const ModerateAbstractionPage: React.FC = () => {
  return <AbstractionPage />;
};

export default ModerateAbstractionPage;
